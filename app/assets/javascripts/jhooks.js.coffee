# Place all the behaviors and hooks related to the matching controller here.
# All this logic will automatically be available in application.js.
# You can use CoffeeScript in this file: http://coffeescript.org/
 jobList = document.getElementById("job-list")
 saveButton = document.getElementsByTagName("submit")
 selectorMenu = document.getElementsByTagName("select")

# #React to Job Row submit button

# 		#--1 print notice to screen -- either "available" or "unavailable"
# 		#   1.a Handled by controller or helper with notice???

# 		#--2 Hide or remove "submit" and "select" buttons/fields from screen after clicking "Save"


# 		#--3 create 'edit' link after submit is clicked, display in place of submit button--routed to edit_jhook_path

# i = 0
# while i < jobList.children.length
# i++ {


# 		}